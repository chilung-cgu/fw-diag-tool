# v1.5.0 Comprehensive Enhancement — Retrospective Implementation Plan

> **Note:** This is a retrospective plan documenting the actual implementation
> that was performed. It follows the structure of writing-plans but was written
> after completion to maintain documentation parity.

> **For agentic workers:** This plan has already been executed. It serves as
> reference documentation for the v1.5.0 changes.

**Goal:** Close functional symmetry gaps across CLI, correlation, session export,
diff export, i18n, dashboard, and documentation.

**Architecture:** 7 independent work streams, each touching a distinct functional
area with no cross-dependencies, executed via parallel subagents.

**Tech Stack:** Python 3.12, Streamlit, Plotly, Typer/Rich (CLI), Pydantic, uv

**Spec:** `docs/superpowers/specs/2026-08-30-v1.5-retrospective-design.md`

## Global Constraints

- Python >= 3.10 compatibility (no tomllib, use tomli)
- All commands via `uv run`
-繁體中文 UI text, English CLI help
- Zero-LaTeX policy
- Git identity from existing config (no override)
- mypy + ruff + pytest must all pass

---

### Task 1: CLI Diff Symmetry — `pcie diff` + `mctp diff`

**Files:**
- Modify: `src/fw_diag_tool/cli.py`
- Create: `tests/test_cli_diff_pcie_mctp.py`

**Status:** ✅ Complete (11 tests)

**Summary:**
- Added `@pcie_app.command("diff")` with Rich table output for AER errors,
  link degradation, vendor/device changes
- Added `@mctp_app.command("diff")` with `--protocol` option (auto/mctp/ipmb),
  Rich table for message/frame deltas, error/warning diffs

---

### Task 2: Correlation UI Extension — PCIe + MCTP

**Files:**
- Modify: `src/fw_diag_tool/gui/pages/correlation_ui.py`
- Modify: `src/fw_diag_tool/gui/shared.py` (add analyze_pcie_input, analyze_mctp_input)
- Modify: `src/fw_diag_tool/resources/__init__.py` (add load_pcie_sample)
- Create: `tests/test_correlation_extended.py`

**Status:** ✅ Complete (11 tests)

**Summary:**
- Extended `build_timeline_events()` for PCIe (BDF, link degradation, AER,
  dmesg) and MCTP (packets, IPMB checksum, sequence mismatch)
- 5-column upload layout, 5-protocol Plotly timeline chart
- Example load buttons for PCIe and MCTP

---

### Task 3: Session Export Symmetry

**Files:**
- Modify: `src/fw_diag_tool/gui/session_io.py`
- Modify: `src/fw_diag_tool/gui/pages/spi_ui.py`
- Modify: `src/fw_diag_tool/gui/pages/uart_ui.py`
- Modify: `src/fw_diag_tool/gui/pages/pcie_ui.py`
- Modify: `src/fw_diag_tool/gui/pages/mctp_ui.py`
- Create: `tests/test_session_export_protocols.py`

**Status:** ✅ Complete (22 tests)

**Summary:**
- Added serialize/replay functions for SPI, UART, PCIe, MCTP in session_io.py
- Each GUI page shows "Save Session" button after successful analysis
- Standard v2 schema with protocol, summary, anomaly_count, SHA-256 signature

---

### Task 4: Diff JSON Export

**Files:**
- Modify: `src/fw_diag_tool/i2c/diff.py` (add to_dict)
- Modify: `src/fw_diag_tool/spi/diff.py` (add to_dict)
- Modify: `src/fw_diag_tool/uart/diff.py` (add to_dict)
- Modify: `src/fw_diag_tool/pcie/diff.py` (add to_dict)
- Modify: `src/fw_diag_tool/mctp/diff.py` (add to_dict)
- Modify: `src/fw_diag_tool/gui/pages/protocol_diff_ui.py`
- Create: `tests/test_diff_json_export.py`

**Status:** ✅ Complete (11 tests)

**Summary:**
- All 5 diff result classes now have to_dict() and to_json()
- Protocol Diff UI has JSON download button alongside Markdown
- Standardized JSON schema with protocol, timestamp, baseline/candidate summary, diff

---

### Task 5: i18n Completeness Audit

**Files:**
- Modify: `src/fw_diag_tool/i18n/domains/gui.py` (~40 keys added)
- Modify: `src/fw_diag_tool/gui/pages/batch_ui.py` (use i18n)
- Modify: `src/fw_diag_tool/gui/pages/settings_ui.py` (use i18n)
- Modify: `src/fw_diag_tool/gui/pages/protocol_diff_ui.py` (use i18n)
- Create: `tests/test_i18n_completeness.py`

**Status:** ✅ Complete (AST-based audit, 23 tests)

**Summary:**
- AST NodeVisitor scans all t() and _tr() calls
- 40+ missing keys added for batch, settings, protocol diff metrics
- Automated completeness test ensures no key is left undefined

---

### Task 6: Dashboard Enhancement

**Files:**
- Modify: `src/fw_diag_tool/gui/pages/dashboard_ui.py`
- Create: `tests/test_dashboard_health_enhanced.py`

**Status:** ✅ Complete (11 tests)

**Summary:**
- Environment health panel: 6-column metrics for Python + 5 dependencies
- Analysis history: Plotly bar chart from MetricsCollector
- Quick session import: load latest .fwsession.json with navigation links

---

### Task 7: README Update

**Files:**
- Modify: `README.md`

**Status:** ✅ Complete

**Summary:**
- 26-page GUI capability matrix (analysis, export, session, diff, i18n)
- v1.5.0 highlights section
- Full CLI command reference with all diff subcommands
- Verified with mkdocs build --strict

---

## Verification Summary

| Check | Result |
|-------|--------|
| pytest | 1034 passed |
| mypy | Success: no issues found in 132 source files |
| ruff | All checks passed |
| New tests added | +57 (977 -> 1034) |

